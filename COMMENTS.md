## Баги/рефактор

- **PolesStore → CatenaryPolesStore** — TODO в коде, но чисто косметически, 25+ мест, отдельный PR.
- В доменных моделях добавили **add** и **remove**, чтобы инкапсулировать структуру данных, при этом доступ осуществляется к структуре
  данных, например `const cp = this.polesStore.poles.get(id);`. Чтобы завершить задачу в полной мере, думаю необходимо в моделях сделать метод `get`. Один вопрос - не возникнут ли баги в работе mobx?
- **HitTestService.hitTestEditTarget** — логика вычисления позиций лейблов частично дублирует Layer-компоненты, но выделение требует анализа всех Layer-файлов.
- **InlineEditService** переименовать в **EntitiesInlineEditService**
- Делать выгрузки в SVG
- Рисовать таблицу с информацией об опорах (согласно распоряжения, отсутствующие данные (нет ещё структуры) пометить "---", а если просто данных нет, хотя структура позволяет их внести, то "-"). Таблица пока что просто отображает данные, редактировать данные в ней пока нельзя.
- возможность формировать сводных отчетов
- В панелях, имеются select. В тех селектах, где кол-во вариантов ограничено (список динамически не формируется) и составляет 2-3 шт., можно сделать SegmentedControl. Вместо двух кликов (раскрыть селект - выбрать), будет один клик - выбрать. При этом места SegmentedControl будет занимать примерно столько же, что и Select. Да я думаю даже для выбора заземления опор в соответствующей панеле можно осуществлять с помощью SegmentedControl.
- Настраиваемый размер анкерной оттяжки

---

## Дорожная карта

### Фаза B: UX и визуал (P2)

- Контекстное меню (ПКМ) — Mantine `Menu`, пункты по контексту выделения
- Магнетизм оси габарита: `SnapService` + пунктирная линия в `PlacementPreview`
- `DisplaySettingsStore` — масштабы/размеры из `constants.ts` в настраиваемый стор
- Аудит `AnchorSection.getCatenaryPoses()` — edge-case с overlap-range
- Массовое создание опор: модалка-таблица (name, x, track, gabarit, side)

### Фаза C: Доменные фичи (P2)

- Inline-редактирование длины пролёта (клик → NumberInput; Shift = сдвинуть цепочку)
- Средние анкеровки — новая сущность `MediumAnchor`, ромб на линии подвески
- Редактирование параметров railway (км.пк начало/конец)

### Фаза D: Вывод (MVP)

- **Таблица-сетка и спецификация** — SVG-таблица над/под планом; заголовки = опоры, строки = габарит/тип/зигзаг

### Фаза E: Рефакторинг (P3–P4)

- Класс `Anchor { type, direction }` вместо `anchorGuy`/`anchorBrace`
- Профилирование рендеринга (per-entity observer-подкомпоненты, React DevTools)
- XLSX импорт опор (SheetJS)
- Трансформаторы/разъединители (новые типы сущностей + Layer)
- Rename `PolesStore` → `CatenaryPolesStore`

---

## Открытые вопросы моделирования

- **Сдвоенные опоры + ригель**: рекомендуется `stanchionCount: 1|2` в `CatenaryPole` (покрывает 95% случаев, визуал — 2 круга рядом). Альтернатива: `PoleAssembly { stanchions[] }` — гибче, но сложнее.
- **Анкер как отдельная сущность**: пока свойство опоры. Сценарий "демонтированный анкер" редок — при необходимости `OrphanAnchor`.
